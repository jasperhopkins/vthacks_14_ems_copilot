AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: 'EMS Copilot -- voice-to-PCR, protocol/dosage assistant, medical translator,
  and drug interaction reference, built on HIPAA-eligible AWS services. See docs/HIPAA_NOTES.md
  before this touches any real patient data: eligible infrastructure + a signed BAA
  is necessary but not sufficient for actual HIPAA compliance.

  '
Parameters:
  Stage:
    Type: String
    Default: dev
  BedrockModelId:
    Type: String
    Default: amazon.nova-pro-v1:0
    Description: Must be a Bedrock model confirmed BAA-covered before real PHI use.
Globals:
  Function:
    Runtime: python3.12
    Timeout: 60
    MemorySize: 256
    Tracing: Active
    Environment:
      Variables:
        ENCOUNTERS_TABLE_NAME:
          Ref: EncountersTable
        AUDIT_TABLE_NAME:
          Ref: AuditLogTable
        PROTOCOL_TABLE_NAME:
          Ref: ProtocolTable
        DRUG_TABLE_NAME:
          Ref: DrugReferenceTable
        AUDIO_BUCKET_NAME:
          Ref: AudioBucket
        BEDROCK_MODEL_ID:
          Ref: BedrockModelId
        KMS_KEY_ARN:
          Fn::GetAtt:
          - EmsCopilotKmsKey
          - Arn
Resources:
  CommonLayer:
    Type: AWS::Serverless::LayerVersion
    Properties:
      LayerName:
        Fn::Sub: ems-copilot-common-${Stage}
      ContentUri: ../../layers/common
      CompatibleRuntimes:
      - python3.12
  EmsCopilotKmsKey:
    Type: AWS::KMS::Key
    Properties:
      Description: CMK for EMS Copilot PHI-adjacent data at rest
      EnableKeyRotation: true
      KeyPolicy:
        Version: '2012-10-17'
        Statement:
        - Sid: AllowAccountAdmin
          Effect: Allow
          Principal:
            AWS:
              Fn::Sub: arn:aws:iam::${AWS::AccountId}:root
          Action: kms:*
          Resource: '*'
  UserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName:
        Fn::Sub: ems-copilot-${Stage}
      MfaConfiguration: OPTIONAL
      EnabledMfas:
      - SOFTWARE_TOKEN_MFA
      Policies:
        PasswordPolicy:
          MinimumLength: 12
          RequireUppercase: true
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: true
      AutoVerifiedAttributes:
      - email
  UserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      UserPoolId:
        Ref: UserPool
      ClientName:
        Fn::Sub: ems-copilot-mobile-${Stage}
      GenerateSecret: false
      ExplicitAuthFlows:
      - ALLOW_USER_SRP_AUTH
      - ALLOW_REFRESH_TOKEN_AUTH
  HttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName:
        Ref: Stage
      CorsConfiguration:
        AllowOrigins:
        - '*'
        AllowHeaders:
        - Authorization
        - Content-Type
        AllowMethods:
        - GET
        - POST
        - OPTIONS
      Auth:
        Authorizers:
          CognitoAuthorizer:
            JwtConfiguration:
              issuer:
                Fn::Sub: https://cognito-idp.${AWS::Region}.amazonaws.com/${UserPool}
              audience:
              - Ref: UserPoolClient
            IdentitySource: $request.header.Authorization
        DefaultAuthorizer: CognitoAuthorizer
  EncountersTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName:
        Fn::Sub: ems-copilot-encounters-${Stage}
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
      - AttributeName: encounter_id
        AttributeType: S
      KeySchema:
      - AttributeName: encounter_id
        KeyType: HASH
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId:
          Ref: EmsCopilotKmsKey
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
  AuditLogTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName:
        Fn::Sub: ems-copilot-audit-log-${Stage}
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
      - AttributeName: encounter_id
        AttributeType: S
      - AttributeName: sort_key
        AttributeType: S
      KeySchema:
      - AttributeName: encounter_id
        KeyType: HASH
      - AttributeName: sort_key
        KeyType: RANGE
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId:
          Ref: EmsCopilotKmsKey
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
  ProtocolTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName:
        Fn::Sub: ems-copilot-protocols-${Stage}
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
      - AttributeName: protocol_id
        AttributeType: S
      KeySchema:
      - AttributeName: protocol_id
        KeyType: HASH
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId:
          Ref: EmsCopilotKmsKey
  DrugReferenceTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName:
        Fn::Sub: ems-copilot-drug-reference-${Stage}
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
      - AttributeName: drug_name
        AttributeType: S
      KeySchema:
      - AttributeName: drug_name
        KeyType: HASH
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId:
          Ref: EmsCopilotKmsKey
  AudioBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName:
        Fn::Sub: ems-copilot-audio-${Stage}-${AWS::AccountId}
      BucketEncryption:
        ServerSideEncryptionConfiguration:
        - ServerSideEncryptionByDefault:
            SSEAlgorithm: aws:kms
            KMSMasterKeyID:
              Ref: EmsCopilotKmsKey
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      VersioningConfiguration:
        Status: Enabled
      LifecycleConfiguration:
        Rules:
        - Id: ExpireRawAudio
          Status: Enabled
          ExpirationInDays: 7
  CloudTrailBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName:
        Fn::Sub: ems-copilot-cloudtrail-${Stage}-${AWS::AccountId}
      BucketEncryption:
        ServerSideEncryptionConfiguration:
        - ServerSideEncryptionByDefault:
            SSEAlgorithm: aws:kms
            KMSMasterKeyID:
              Ref: EmsCopilotKmsKey
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
  CloudTrailBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket:
        Ref: CloudTrailBucket
      PolicyDocument:
        Statement:
        - Sid: AWSCloudTrailAclCheck
          Effect: Allow
          Principal:
            Service: cloudtrail.amazonaws.com
          Action: s3:GetBucketAcl
          Resource:
            Fn::GetAtt:
            - CloudTrailBucket
            - Arn
        - Sid: AWSCloudTrailWrite
          Effect: Allow
          Principal:
            Service: cloudtrail.amazonaws.com
          Action: s3:PutObject
          Resource:
            Fn::Sub: ${CloudTrailBucket.Arn}/AWSLogs/${AWS::AccountId}/*
          Condition:
            StringEquals:
              s3:x-amz-acl: bucket-owner-full-control
  EmsCopilotTrail:
    Type: AWS::CloudTrail::Trail
    DependsOn: CloudTrailBucketPolicy
    Properties:
      TrailName:
        Fn::Sub: ems-copilot-${Stage}
      S3BucketName:
        Ref: CloudTrailBucket
      IsLogging: true
      IsMultiRegionTrail: true
      IncludeGlobalServiceEvents: true
      EnableLogFileValidation: true
      EventSelectors:
      - ReadWriteType: All
        IncludeManagementEvents: true
  EncounterFunctionsRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
        - Effect: Allow
          Principal:
            Service: lambda.amazonaws.com
          Action: sts:AssumeRole
      ManagedPolicyArns:
      - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
      - PolicyName: EmsCopilotAppAccess
        PolicyDocument:
          Version: '2012-10-17'
          Statement:
          - Sid: EncountersReadWrite
            Effect: Allow
            Action:
            - dynamodb:PutItem
            - dynamodb:GetItem
            - dynamodb:Query
            - dynamodb:Scan
            Resource:
            - Fn::GetAtt:
              - EncountersTable
              - Arn
            - Fn::GetAtt:
              - ProtocolTable
              - Arn
            - Fn::GetAtt:
              - DrugReferenceTable
              - Arn
          - Sid: AuditAppendOnly
            Effect: Allow
            Action:
            - dynamodb:PutItem
            Resource:
              Fn::GetAtt:
              - AuditLogTable
              - Arn
          - Sid: KmsUse
            Effect: Allow
            Action:
            - kms:Encrypt
            - kms:Decrypt
            - kms:GenerateDataKey
            - kms:DescribeKey
            Resource:
              Fn::GetAtt:
              - EmsCopilotKmsKey
              - Arn
          - Sid: S3Audio
            Effect: Allow
            Action:
            - s3:PutObject
            - s3:GetObject
            Resource:
              Fn::Sub: ${AudioBucket.Arn}/*
          - Sid: S3AudioBucket
            Effect: Allow
            Action:
            - s3:ListBucket
            Resource:
              Fn::GetAtt:
              - AudioBucket
              - Arn
          - Sid: TranscribeAccess
            Effect: Allow
            Action:
            - transcribe:StartTranscriptionJob
            - transcribe:GetTranscriptionJob
            Resource: '*'
          - Sid: BedrockInvoke
            Effect: Allow
            Action:
            - bedrock:InvokeModel
            Resource: '*'
          - Sid: ComprehendMedicalAccess
            Effect: Allow
            Action:
            - comprehendmedical:InferRxNorm
            - comprehendmedical:DetectEntitiesV2
            Resource: '*'
          - Sid: TranslateAndPolly
            Effect: Allow
            Action:
            - translate:TranslateText
            - polly:SynthesizeSpeech
            Resource: '*'
  PcrFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: PcrFunction
      Handler: app.handler
      Role:
        Fn::GetAtt:
        - EncounterFunctionsRole
        - Arn
      Layers:
      - Ref: CommonLayer
      Events:
        GenerateApi:
          Type: HttpApi
          Properties:
            ApiId:
              Ref: HttpApi
            Path: /pcr/generate
            Method: POST
    Metadata:
      SamResourceId: PcrFunction
  PcrUploadUrlFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: PcrUploadUrlFunction
      Handler: upload_url.handler
      Role:
        Fn::GetAtt:
        - EncounterFunctionsRole
        - Arn
      Layers:
      - Ref: CommonLayer
      Events:
        UploadUrlApi:
          Type: HttpApi
          Properties:
            ApiId:
              Ref: HttpApi
            Path: /pcr/upload-url
            Method: GET
    Metadata:
      SamResourceId: PcrUploadUrlFunction
  PcrStatusFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: PcrStatusFunction
      Handler: status.handler
      Role:
        Fn::GetAtt:
        - EncounterFunctionsRole
        - Arn
      Layers:
      - Ref: CommonLayer
      Events:
        StatusApi:
          Type: HttpApi
          Properties:
            ApiId:
              Ref: HttpApi
            Path: /pcr/{encounter_id}
            Method: GET
    Metadata:
      SamResourceId: PcrStatusFunction
  ProtocolFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ProtocolFunction
      Handler: app.handler
      Role:
        Fn::GetAtt:
        - EncounterFunctionsRole
        - Arn
      Layers:
      - Ref: CommonLayer
      Events:
        QueryApi:
          Type: HttpApi
          Properties:
            ApiId:
              Ref: HttpApi
            Path: /protocol/query
            Method: POST
    Metadata:
      SamResourceId: ProtocolFunction
  TranslateFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: TranslateFunction
      Handler: app.handler
      Role:
        Fn::GetAtt:
        - EncounterFunctionsRole
        - Arn
      Layers:
      - Ref: CommonLayer
      Events:
        TranslateApi:
          Type: HttpApi
          Properties:
            ApiId:
              Ref: HttpApi
            Path: /translate
            Method: POST
    Metadata:
      SamResourceId: TranslateFunction
  DrugLookupFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: DrugLookupFunction
      Handler: app.lookup_handler
      Role:
        Fn::GetAtt:
        - EncounterFunctionsRole
        - Arn
      Layers:
      - Ref: CommonLayer
      Events:
        LookupApi:
          Type: HttpApi
          Properties:
            ApiId:
              Ref: HttpApi
            Path: /drug/lookup
            Method: POST
    Metadata:
      SamResourceId: DrugLookupFunction
  DrugInteractionFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: DrugInteractionFunction
      Handler: app.interaction_handler
      Role:
        Fn::GetAtt:
        - EncounterFunctionsRole
        - Arn
      Layers:
      - Ref: CommonLayer
      Events:
        InteractionApi:
          Type: HttpApi
          Properties:
            ApiId:
              Ref: HttpApi
            Path: /drug/check-interaction
            Method: POST
    Metadata:
      SamResourceId: DrugInteractionFunction
Outputs:
  ApiUrl:
    Description: Base URL for the API -- put this in mobile/src/config.js
    Value:
      Fn::Sub: https://${HttpApi}.execute-api.${AWS::Region}.amazonaws.com/${Stage}
  UserPoolId:
    Value:
      Ref: UserPool
  UserPoolClientId:
    Value:
      Ref: UserPoolClient
  AudioBucketName:
    Value:
      Ref: AudioBucket
  KmsKeyArn:
    Value:
      Fn::GetAtt:
      - EmsCopilotKmsKey
      - Arn
